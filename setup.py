#!/usr/bin/env python
from distutils.core import setup
setup(name='p_test',
      version='0.1.0',
      description="p_test us just used to test building and packaging python with jenkins"
      # py_modules=['p_test'],
      )
